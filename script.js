// Creating an object for the journal entry
const journalEntry = {
    date: "test",
    concepts: "test",
    entry: "test",
    mood: "test"
}

// Creating an array to store the journal entries.
let entryCollection = []

// Pushing a test object to the array
entryCollection.push(journalEntry)
// console.log(entryCollection);